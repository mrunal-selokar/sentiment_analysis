<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Hotel Advisor", root_path, id: "logo" %> 
      <nav>       
        <ul class="nav navbar-nav navbar-right"> 
          <li><a href="/"><span class="glyphicon glyphicon-home"></span> Home</a></li>
          <% if logged_in? %>
            <% if admin? %>
              <li><a href="/users/"><i class="fa fa-users" aria-hidden="true"></i> Users</a></li>
            <% end %>
            <li><%= link_to "Hotels", hotels_path %></li>
            <li class="dropdown"> <!--<span class="img-nav"><%= image_tag(current_user.avatar.url(:thumb)) %></span>-->
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <span class="glyphicon glyphicon-user"></span>
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to "Log out", logout_path, method: "delete" %></li>
              </ul>
            </li>
            <%else %>
              <li><a href="login"><span class="glyphicon glyphicon-log-in"></span> Log in</a>
              </li>
            <% end %>
          </ul>
      </nav>
  </div>
</header>