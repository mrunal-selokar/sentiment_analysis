<% provide(:title, @user.name) %>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
    <div>
      <section class="user_info">
        <h1>
          <span class="avatar" ><%= image_tag(current_user.avatar.url(:thumb)) %></span>
          <%= @user.name %> <br>
        </h1>
        <h4>
          <span class="badge"><%= @user.reviews.count %></span> Reviews
        </h4>
      </section>
      </div>
      <% if @user == current_user %>
        <ul class="nav nav-tabs">
          <li><a data-toggle="tab" href="#reviewform"><i class="fa fa-comment-o" aria-hidden="true"></i> Review a Hotel</a></li>
          <% if current_user.admin?%>
          <li><a data-toggle="tab" href="#hotelform">Add New Hotel</a></li><% end %>
        </ul>
        <div class="tab-content">
        <div id="reviewform" class="tab-pane fade">
          <section class="review_form">
            <%= render 'shared/review_form' %>
          </section>
        </div>
        <div id="hotelform" class="tab-pane fade">
          <section class="review_form">
            <%= render 'shared/hotel_form' %>
          </section>
        </div>
      </div>
      <% if current_user.admin?%>
        <button type="button" class="btn btn-lg" data-toggle="modal" data-target="#addHotel"><span class="glyphicon glyphicon-plus"></span> Add new Hotel</button>
        <div id="addHotel" class="modal fade" role="dialog">
          <div class="modal-dialog modal-sm">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Add a new Hotel</h4>
              </div>
              <div class="modal-body">
                <%= render 'shared/hotel_form'%>
              </div>
              <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
      <% end %>

    </aside>

    <div class="col-md-8">
        <h3>My Reviews <i class="fa fa-comments-o" aria-hidden="true"></i></h3>
        <%= render 'shared/feed' %>
    </div>
  </div>
<% end %>